# screenshots/

วางไฟล์ภาพหน้า Firebase Console (เห็น `formulas` ≥5 รายการ) ไว้ที่นี่ — นี่คือ **ต้นฉบับ**

เมื่อวางแล้ว จะมีสำเนาไฟล์เดียวกันไปไว้ที่ [`../../docs/06-module2-homework/`](../../docs/06-module2-homework/) ด้วย เพราะโจทย์การบ้านเช็คจากโฟลเดอร์ `docs/` ของ repo โดยตรง
