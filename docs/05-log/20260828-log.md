# Log — 2026-08-28

## สรุปงานที่ทำ

รับโจทย์งานส่งวิชา RAISE (W4): สร้าง High-Level Architecture, API Spec + Database Schema, Detailed Design, และ Non-Functional Requirement Review ตรวจพบว่า `architecture.md`/`api-spec.md`/`db-spec.md` ที่มีอยู่แล้ว (จาก log 2026-08-18) เขียนขึ้นก่อนที่ FR-40/FR-41/FR-42 จะถูกเพิ่มเข้า `backlog.md` — จึงต้อง resync ทั้ง 5 เอกสารก่อน (feature-list → user-journey → architecture → api-spec/db-spec) แล้วจึงทำ Detailed Design (ขอบเขต MVP) และ NFR Review ต่อ

**การตัดสินใจร่วมกับผู้ใช้ก่อนเริ่มงาน:**

| # | คำถาม | คำตอบที่เลือก |
|---|---|---|
| 1 | จัดการ FR-40/41/42 + OI-05/OI-06/UJ-03 อย่างไรก่อนเริ่ม Detailed Design | **Sync เต็มรูปแบบ** — ปิดช่องว่างทั้งหมด |
| 2 | ขอบเขต Detailed Design | **เฉพาะ MVP** — F-01–F-08 + F-18 (ฟีเจอร์สารต้องห้ามใหม่) |
| 3 | ทำ NFR Review หรือไม่ | **ทำ** |
| 4 | รูปแบบไฟล์ส่งงาน | **Markdown ตรงๆ จาก `docs/02-design/02-technical/`** ไม่แปลงเป็น HTML |

---

## 1. Gate ตรวจความสอดคล้องก่อนเริ่ม

เทียบ `feature-list.md`/`user-journey.md`/`architecture.md`/`api-spec.md`/`db-spec.md` (เวอร์ชัน 2026-08-18) กับ `backlog.md` ล่าสุด (42 FR) — พบว่าทั้ง 5 ไฟล์ **ไม่มีการกล่าวถึง FR-40/FR-41/FR-42 เลย** (ยืนยันด้วยการ grep ตรงๆ) และ OI-05 (Organization entity), OI-06 (Formulation Manager journey), UJ-03 (PDPA consent journey) ยังเปิดค้างอยู่ทั้งหมด → ไม่ผ่าน gate จึง resync ก่อนดำเนินการต่อ

## 2. Resync `feature-list.md` และ `user-journey.md`

| ไฟล์ | สิ่งที่เพิ่ม/แก้ |
|---|---|
| `docs/02-design/feature-list.md` | เพิ่ม **F-18 การป้องกันสารต้องห้าม** (Must have, FR-40/41) และ **F-19 มุมมองภาพรวมหลายสูตร** (Should have, FR-42) เข้าตารางสรุป + หัวข้อรายละเอียด อัปเดตยอดรวมเป็น Must 9 · Should 8 · Could 2 · Won't 5 |
| `docs/02-design/user-journey.md` | แทรก step ตรวจ/บล็อกสารต้องห้าม (FR-40/41) เข้า **UJ-01** ระหว่างขั้น "เลือกสาร" กับ "ระบุสัดส่วน" (renumber 16→18 ขั้น) · เพิ่ม **UJ-03 จัดการความยินยอม PDPA** (ปิดช่องว่าง FR-39 ที่ traceability เดิมระบุว่ายังไม่มี journey) · เพิ่ม **UJ-04 มุมมองภาพรวมหลายสูตรของ Formulation Manager** (ปิด OI-06) · อัปเดต Persona รอง "เค" (Formulation Manager) · อัปเดตตาราง Traceability |

**ไม่พบ NEEDS_NEW_REQUIREMENT ใหม่** ระหว่างขั้นนี้ — FR-40/41/42 มีอยู่แล้วใน backlog ครบ

## 3. Resync `architecture.md`

| ส่วน | สิ่งที่เพิ่ม/แก้ |
|---|---|
| Component Diagram | เพิ่มเส้น `FM → REG` (Formula Management ตรวจ Regulatory Ruleset Store โดยตรงสำหรับ FR-40/41) · เพิ่ม actor `Formulation Manager` |
| §3.2 Identity, Access & Audit Module | เพิ่มความรับผิดชอบ **Organization Scoping** (FR-42) — ระบุชัดว่าเป็นข้อยกเว้นเดียวที่อนุญาตต่อ NFR-09 (BR-08) |
| §3.3 Formula Management Module | เพิ่มความรับผิดชอบ **Prohibited Substance Guard** (FR-40/41, BR-07) และ **Multi-Formula Overview** (FR-42) |
| §3.9 Primary Data Store | เพิ่มการเก็บข้อมูล Organization |
| §4 Data Flow | อัปเดต sequence diagram UJ-01 ให้มีขั้นตรวจ/บล็อกสารต้องห้าม · เพิ่ม sequence diagram ใหม่ **§4.3 UJ-04** (Formulation Manager overview + organization scoping) |
| §5 NFR Mapping | อัปเดตแถว NFR-09 ให้ระบุข้อยกเว้น FR-42/BR-08 พร้อมหมายเหตุว่ารอ nfr-review ยืนยันถ้อยคำอย่างเป็นทางการ |
| §6 ประเด็นรอตัดสินใจ | เพิ่มข้อ 9 เรื่องกลไก enforce Organization Scoping จริง (รอ technology-stack.md) |

**Gate ตรวจก่อนเขียน:** เทียบ `feature-list.md`/`user-journey.md` (resync แล้วในขั้น 2) กับ `backlog.md` — สอดคล้องครบ FR-01 ถึง FR-42 จึงผ่าน gate

**ไม่พบ NEEDS_NEW_REQUIREMENT ใหม่**

## 4. Resync `api-spec.md` และ `db-spec.md`

| ไฟล์ | สิ่งที่เพิ่ม/แก้ |
|---|---|
| `db-spec.md` | เพิ่ม entity **`Organization`** (ปิด OI-05) + attribute **`User.organization_ref`** · อัปเดต ER Diagram · อัปเดตหมายเหตุ `RegulatoryLimit` ให้ชี้ไปยัง FR-40/FR-41 ที่ปิดช่องว่างเดิมแล้ว |
| `api-spec.md` | อัปเดต `SearchSubstance` ให้คืน `regulatory_status`/`regulatory_reason` (FR-40) · อัปเดต `UpdateFormulaComposition` ให้ปฏิเสธสาร PROHIBITION ไม่มี override (FR-41, BR-07) · เพิ่ม **โมดูล H — Formulation Manager: Organization Overview** พร้อม operation ใหม่ `ListOrganizationFormulas` (FR-42, บังคับกรองด้วย organization เสมอ ตาม BR-08) · อัปเดตตารางสรุปโมดูล (32 → 33 operation) |

**Gate ตรวจก่อนเขียน:** เทียบ `architecture.md` (resync แล้ว) กับ feature ที่ต้องออกแบบ — ครอบคลุมครบจึงผ่าน gate

**ไม่พบ NEEDS_NEW_REQUIREMENT ใหม่** — ตรวจ field ระหว่าง `api-spec.md`/`db-spec.md` ให้ตรงกัน (`organization_ref`, `regulatory_status` ไม่ปรากฏใน db-spec แบบชื่ออื่น) และตรวจว่าไม่มี HTTP method/path, SQL type, หรือชื่อ technology หลุดเข้าไปทั้งสองไฟล์ (grep แล้ว)

## 5. Detailed Design (ขอบเขต MVP — F-01–F-08, F-18)

สร้างโฟลเดอร์ `docs/02-design/02-technical/detailed-design/` ใหม่ (ไม่เคยมีมาก่อน) พร้อมไฟล์ 9 ไฟล์ แต่ละไฟล์มี Sequence Diagram, ตาราง Operation ↔ Entity, State Diagram (ถ้ามี), และ Edge Case ที่อ้างอิงกลับไป `acceptance-criteria.md`/spec จริง:

| ไฟล์ | ฟีเจอร์ |
|---|---|
| `formula-management.md` | F-01 |
| `engine-a-calculation.md` | F-02 |
| `olfactory-threshold-filter.md` | F-03 |
| `engine-b-aroma-description.md` | F-04 |
| `ifra-compliance-check.md` | F-05 |
| `muddy-accord-detection.md` | F-06 |
| `dashboard-aroma-profile.md` | F-07 (ตั้งชื่อไฟล์ตรงกับ `test-cases/dashboard-aroma-profile.md` ที่มีอยู่แล้ว) |
| `account-identity-audit-consent.md` | F-08 |
| `prohibited-substance-guard.md` | F-18 |

**ขอบเขตที่ตัดออกโดยตั้งใจ:** F-09 ถึง F-17, F-19 (ทั้งหมดเป็น Should/Could have) ยังไม่มี detailed-design ตามที่ผู้ใช้เลือกขอบเขต MVP-only — บันทึกไว้ในหมายเหตุของแต่ละไฟล์ที่เกี่ยวข้อง (เช่น `muddy-accord-detection.md` ระบุชัดว่า F-10/FR-24 ยังไม่รวมอยู่)

**หมายเหตุคุณภาพ:** `prohibited-substance-guard.md` (F-18) ระบุไว้ตรงๆ ว่ายังไม่มี Acceptance Criteria/Test Case ผูกไว้ เพราะ FR-40/41 เป็นความต้องการที่เพิ่งปิดช่องว่างในรอบนี้ — แนะนำรัน `sync-test-plan` เพิ่มเติมในรอบถัดไป (นอกขอบเขตงานที่ทำวันนี้)

## 6. NFR Review

สร้าง `docs/02-design/02-technical/nfr-review.md` ใหม่ (ไม่เคยมีมาก่อน) ตรวจ NFR ทั้ง 15 ข้อเทียบกับเอกสารเชิงเทคนิคที่ resync แล้วทั้งหมด + detailed-design 9 ไฟล์

**ผลสรุป:** รองรับแล้ว 13/15 · รองรับแล้วมีเงื่อนไขให้ติดตาม 1/15 (**NFR-09**) · รองรับบางส่วน 1/15 (**NFR-13**) · ไม่พบ NFR ที่ยังไม่รองรับเลย

**ประเด็นสำคัญที่พบ — NFR-09:** ชั้นออกแบบเชิงเทคนิค (architecture/api-spec/db-spec) implement ข้อยกเว้นสำหรับ FR-42 (Formulation Manager) ครบแล้ว แต่ถ้อยคำจริงใน `20260818-01-ai-perfumery-core.md` §5 ยังไม่ถูกแก้ไข — nfr-review แนะนำให้ผู้ใช้/PO อนุมัติข้อเสนอปรับถ้อยคำที่ระบุไว้ใน [[../01-requirements/01-spec/20260818-03-formulation-manager-role|Formulation Manager Role Spec §3]] อย่างเป็นทางการ (nfr-reviewer ไม่มีสิทธิ์แก้ spec เอง)

**ประเด็นที่เหลือ — NFR-13:** ส่วน "Fallback Plan" ทางเทคนิครองรับแล้วเต็มที่ (`is_fallback` flag) แต่ส่วน "ระบุผู้รับผิดชอบ" เป็นประเด็นกระบวนการ/องค์กรที่ architecture ระดับนี้ให้ได้แค่ hook ทางเทคนิค — ไม่ใช่ช่องว่างที่ sync เอกสารเทคนิคเพิ่มแล้วจะแก้ได้

## 7. การตัดสินใจสำคัญของวันนี้

| # | เรื่อง | ตัดสินใจว่า | เหตุผล |
|---|---|---|---|
| D-11 | รูปแบบความสัมพันธ์ `User` ↔ `Organization` | 1 ผู้ใช้ต่อ 1 องค์กร (ไม่ใช่ N:M) | FR-42 ต้องการแค่ "บัญชี/องค์กรเดียวกัน" ไม่มีความต้องการให้ผู้ใช้สังกัดหลายองค์กรพร้อมกัน — ถ้าต้องการในอนาคตต้องออกแบบ join entity เพิ่ม |
| D-12 | `ListOrganizationFormulas` รับพารามิเตอร์ขอบเขตองค์กรจากภายนอกหรือไม่ | **ไม่รับ** — ใช้ `organization_ref` ของผู้เรียกปัจจุบันเสมอ | ป้องกันการปลอมค่ามาอ่านข้ามองค์กร ตรงตาม BR-08 ที่ห้ามข้อยกเว้นเด็ดขาด |
| D-13 | Prohibited Substance Guard ตรวจซ้ำที่ backend หรือพึ่ง badge ฝั่ง Client อย่างเดียว | **ตรวจซ้ำที่ `UpdateFormulaComposition` (backend) เสมอ** ไม่พึ่ง badge ฝั่ง Client เป็นกลไกป้องกันเดียว | BR-07 ระบุว่าห้าม override เด็ดขาด — การพึ่ง UI อย่างเดียวเสี่ยงถูกข้ามได้หากมีช่องทางเรียก operation ตรงๆ |
| D-14 | ขอบเขต Detailed Design | จำกัดเฉพาะ 9 ฟีเจอร์ Must-have ตามที่ผู้ใช้เลือก | ตรงกับ MVP framing ที่ใช้มาตลอดทั้งโปรเจกต์ ลดขอบเขตงานให้ส่งได้ทันตามรอบ |

## 8. ขั้นตอนถัดไป (แนะนำ ไม่ได้ทำในรอบนี้)

- [ ] ผู้ใช้/PO ตัดสินใจเรื่องข้อเสนอปรับถ้อยคำ NFR-09 (ดู nfr-review.md §2) แล้วแก้ `20260818-01-ai-perfumery-core.md` โดยตรงถ้าอนุมัติ
- [ ] รัน `sync-test-plan` เพื่อเพิ่ม Acceptance Criteria + Test Case ให้ F-18 (Prohibited Substance Guard) และ F-19 (Formulation Manager Overview) ที่ยังไม่มี AC ผูกไว้
- [ ] ถ้าต้องการ Detailed Design ของฟีเจอร์ Should/Could have (F-09–F-17, F-19) เพิ่มเติม ให้รัน `sync-detailed-design` ต่อ
- [ ] `git add` + `commit` + `push` เอกสารทั้งหมดที่แก้ไข/สร้างใหม่ในรอบนี้ขึ้น GitHub
