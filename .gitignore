# ============================================================
# ป้องกันข้อมูลภายในหลุดออกนอกเครื่อง
# repo นี้ใช้ส่งงานวิชา RAISE เท่านั้น — ห้าม commit เอกสารเชิงธุรกิจ/IP
# ============================================================

# เอกสารต้นทางเชิงลึกของโครงการ (R&D / IP) — ใช้ในเครื่องเท่านั้น ห้าม push
reference/

# เอกสารสัญญา / ธรรมาภิบาล / ทรัพย์สินทางปัญญา (ห้ามเข้า repo เด็ดขาด)
*MOU*
*MoU*
*CONTRACT*
*GOVERNANCE*
*BOUNDARY_RULES*
*ADVISOR*
*EQUITY*

# ข้อมูลดิบ / ฐานข้อมูลสาร / ราคา
Database/
PriceRM/
*.csv
*.xls
*.xlsx
*.zip

# ข้อมูลส่วนบุคคล / ความลับ
*.env
cookie.txt
*secret*
*credential*

# คีย์ AI (OpenRouter) — ห้ามขึ้น GitHub เด็ดขาด (Module 2 การบ้านที่ 3 / สัปดาห์ที่ 8)
# ⚠️ ไม่รวมไฟล์ *-example.js ที่เป็นแม่แบบไม่มีคีย์จริง (git ignore ไม่ match เพราะชื่อไฟล์ต่างกัน)
config.local.js
config.local.json

# dependencies
node_modules/

# Firebase CLI local deploy cache (regenerated by `firebase deploy`, not project source)
.firebase/

# ระบบ
.obsidian/workspace.json
.DS_Store
Thumbs.db
desktop.ini
__pycache__/
*.pyc

# ชุดไฟล์ส่งงาน (สร้างด้วย tools/build-submission.py) — ไม่ต้อง commit
Submission-RAISE-W3/
